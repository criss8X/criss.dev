---
import Card from "@ui/card.astro";
import { Clock, MapPin } from "@ui/icon.astro";
import Section from "@/components/section.astro";
import { parseHighlightsFromParagraph } from "@/lib/markdown";

const aboutMeParagraph = `Con **4 años de experiencia** en ingeniería de software,
he perfeccionado un enfoque único que combina **metodologías ágiles** con **tecnologías
de vanguardia**. Mi especialidad es crear soluciones que no solo funcionan,
sino que escalan y generan valor real para el negocio.`;

const paragraphHtml = parseHighlightsFromParagraph(aboutMeParagraph);
---

<Section id="about" title="Sobre mí" className="max-w-4xl">
  <Card className="bg-card/50 backdrop-blur border-border/50">
    <Card variant="content" className="pt-8 pb-8">
      <div class="space-y-6">
        <p
          class="text-lg leading-relaxed text-muted-foreground text-center max-w-3xl mx-auto"
          set:html={paragraphHtml}
        />

        <div
          class="flex items-center justify-center gap-6 pt-4 text-sm text-muted-foreground"
        >
          <div class="flex items-center gap-2">
            <MapPin class="h-4 w-4 text-purple-400" />

            <span>Puerto Padre, Las Tunas, Cuba</span>
          </div>

          <div class="flex items-center gap-2">
            <Clock class="h-4 w-4 text-purple-400" />

            <span>GMT-5 (Flexible)</span>
          </div>
        </div>
      </div>
    </Card>
  </Card>
</Section>
